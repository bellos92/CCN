# Copilot Instructions

## Project Guidelines
- Team preference: repositories should expose only CRUD operations and simple data queries; business logic must live in services. Use ViewModels for presentation layers instead of passing Entities directly.